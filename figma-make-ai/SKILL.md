---
name: figma-make-ai
description: "Figma Make AI 提示词生成器。自动为产品/功能需求生成可直接用于 Figma Make AI 的中文提示词，采用单页面生成策略。适用于产品/设计/开发协作场景。"
version: 3.1.0
last_updated: 2026-03-24
---

# Figma Make AI 提示词生成器 v3.0

## 核心特性

- 单页面生成，每个页面独立生成提示词
- 移动端 375×812px，pc端 1280×800px（固定高度，超出内容主区域滚动）
- 结合 /ui-ux-pro-max skill 生成设计指导
- 页面提示词按关联性紧密度排序（核心优先）

---

## 使用说明

**触发方式：** 直接请求、描述需求、或使用 `/figma-make-ai` 命令

**上下文：** 从用户提供的需求描述（一句话想法/PRD/用户流程/功能列表）中提取信息

---

## 全局规则

**输出格式：** 纯文本 Markdown，中文内容，不使用图片/链接/表格/emoji

**页面定义：** 每个交互状态都是一个独立页面（主功能页、弹窗、成功/失败提示、加载/空状态等）

**尺寸规范：**
- 移动端：375×812px（固定）
- pc端：1280×800px（固定）
- 滚动：主内容区域滚动（使用 min-h-0 + scrollbar-hide 方案隐藏滚动条），页面整体固定
- 自定义：用户明确提供的尺寸优先

**安全区规范（小程序/App端）：**
- 顶部安全区：状态栏区域（约44px），内容需避开或使用 safe-area-inset-top
- 底部安全区：导航栏/Home指示器区域（约34px），内容需避开或使用 safe-area-inset-bottom
- 胶囊按钮：右上角系统胶囊（约32×18px），页面布局需避免与之重叠
- 安全区约束需在【全局强制约束】中声明

**禁止事项：** 不输出设计理念、不省略交互、不假设技术实现、不使用相对宽度

---

## 执行流程

1. 需求理解与确认 → 确定尺寸 → 列出页面清单 → 与用户确认
2. 调用 /ui-ux-pro-max 获取设计风格指导
3. 按关联性紧密度排序：核心功能页 → 子功能页 → 交互状态页 → 辅助状态页
4. 为每个页面独立生成 Figma Make AI 提示词
5. 验证生成结果（尺寸、滚动规则、强制约束声明）

---

## 强制约束声明

```
【全局强制约束】
- 画板宽度严格等于 [宽度]px，不得自动调整
- 画板高度固定为 [高度]px，不得自动调整
- 滚动规则：主内容区域高度超过固定高度时，主内容区域内部滚动展示（使用 min-h-0 + scrollbar-hide 方案隐藏滚动条），页面整体高度固定不变
- 导航栏和底部Tab栏固定定位，不随内容滚动
- 所有元素宽度不得超过 [宽度]px

【小程序/App端额外约束】
- 顶部安全区：状态栏区域约44px，内容布局需避开或使用 safe-area-inset-top
- 底部安全区：导航栏/Home指示器区域约34px，内容布局需避开或使用 safe-area-inset-bottom
- 胶囊按钮：右上角系统胶囊约32×18px，页面内容不得遮挡
```

---

## 提示词结构

```
### [页面名称]

**Figma Make AI 提示词：**

**【全局强制约束】**
[约束声明]

**页面类型：** [移动端/桌面端]
**尺寸：** [宽度] × [高度] px（固定高度，超出内容主区域滚动）

**页面描述：** [一句话说明页面用途]

**布局结构：**
- 顶部固定区域：[内容]（不滚动）
- 中间主体区域：[内容]（可滚动）
- 底部固定区域：[内容]（不滚动）

**具体元素：**
1. [元素名称]
   - 类型：[按钮/文本/输入框/图片/图标等]
   - 内容：[具体文案]
   - 样式：[颜色、大小、位置]

**交互说明：**
- 触发条件：[从哪个页面/状态来]
- 当前页面行为：[页面显示什么]
- 后续转换：[每个操作元素的结果]

**视觉风格：**
[色彩方案、字体层级、圆角规范、间距规范]
```

---

## 设计一致性

在第一个页面详细定义视觉风格规范，后续页面引用并复用这些规范。使用 /ui-ux-pro-max 生成统一的设计指导。

---

## 状态检查

确保覆盖：初始状态、加载中、成功/失败、空状态、表单验证状态、二次确认、用户引导等

---

## 单页面提示词示例

```
请生成一个移动端页面。

【全局强制约束】
- 画板宽度严格等于 375px，不得自动调整
- 画板高度固定为 812px，不得自动调整
- 滚动规则：主内容区域高度超过固定高度时，主内容区域内部滚动展示（使用 min-h-0 + scrollbar-hide 方案隐藏滚动条）
- 导航栏固定在顶部，不随内容滚动
- 所有元素宽度不得超过 375px

**页面类型：** 移动端
**尺寸：** 375 × 812 px（固定高度，超出内容主区域滚动）

**视觉风格：** 主色 #1890FF，危险色 #FF4D4F，文本色 #333333/#666666

**页面描述：** 展示用户的订单列表，每条订单可进行删除操作

**布局结构：** 顶部导航栏 44px，中间订单列表（可滚动），底部加载更多 50px

**具体元素：** [详细描述每个元素]

**交互说明：** 用户进入订单管理页面 → 显示订单列表 → 点击删除按钮 → 删除确认弹窗
```

**详细模板和示例请参阅** [references/templates.md](references/templates.md)

---

## 小程序/App端安全区示例

```
请生成一个小程序端页面。

【全局强制约束】
- 画板宽度严格等于 375px，不得自动调整
- 画板高度固定为 812px，不得自动调整
- 滚动规则：主内容区域高度超过固定高度时，主内容区域内部滚动展示（使用 min-h-0 + scrollbar-hide 方案隐藏滚动条）
- 导航栏固定在顶部，不随内容滚动
- 所有元素宽度不得超过 375px

【小程序/App端额外约束】
- 顶部安全区：状态栏区域约44px，内容布局需避开或使用 safe-area-inset-top
- 底部安全区：导航栏/Home指示器区域约34px，内容布局需避开或使用 safe-area-inset-bottom
- 胶囊按钮：右上角系统胶囊约32×18px，页面内容不得遮挡

**页面类型：** 移动端（小程序）
**尺寸：** 375 × 812 px（固定高度，超出内容主区域滚动）

**页面描述：** 展示商品列表，支持下拉刷新和上拉加载更多

**布局结构：**
- 顶部固定区域：状态栏占位44px + 顶部导航栏44px（不滚动）
- 中间主体区域：商品列表（可滚动）
- 底部固定区域：底部Tab栏50px + 底部安全区34px

**具体元素：** [详细描述每个元素]

**交互说明：** 用户进入商品列表页 → 显示商品列表 → 下拉刷新 → 加载最新商品
```

---

## 输出要求

- 纯文本 Markdown，不使用图片/链接/表格/emoji
- UTF-8 编码，全中文内容
- 遵循标准化提示词模板，必填字段不省略
- 禁止：设计理论讨论、多种方案选择、技术实现假设、模糊描述
- 必需：明确尺寸、十六进制色值、带单位 px 的数值、完整元素列表、清晰交互说明

---

## 故障排查

**详细故障排查指南请参阅** [references/troubleshooting.md](references/troubleshooting.md)

---

## 完成标准

- 尺寸：移动端 375×812px，pc端 1280×800px，固定高度
- 约束：包含【全局强制约束】声明，宽度/高度固定，滚动规则明确
- 内容：交互说明含触发条件/当前行为/后续转换，元素描述含类型/内容/样式/位置
- 排序：核心页面优先 → 子功能页面 → 交互状态页面 → 辅助状态页面
- 风格：视觉规范完整，多页面一致（通过 /ui-ux-pro-max 统一）
- 格式：中文纯文本 Markdown，无图片/链接/emoji，十六进制色值，带单位 px
